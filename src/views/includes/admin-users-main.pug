input#search(type="search", name="search", placeholder="Search id or username...")
h1 Users #[span#addTicket.add.glyphicon.glyphicon-plus(aria-hidden="true")]
if(message)  
  p Message...
table
  thead
    tr
      th ID
      th Name
      th Email
      th Role
      th Created
      th Actions
  tbody
    each user in userList
      tr
        td=user.id
        td=user.name
        td=user.email
        td=user.role
        td=user.created.toLocaleString('en-GB')
        td 
          a(href="#", style="margin-right:20px", onclick="document.getElementById('edit').style.display = 'block';var data = this.parentElement.previousElementSibling.previousElementSibling;document.getElementById('editrole').value = data.innerHTML;data = data.previousElementSibling;document.getElementById('editemail').value = data.innerHTML;data = data.previousElementSibling;document.getElementById('editname').value = data.innerHTML;data = data.previousElementSibling;document.getElementById('editid').value = data.innerHTML;")
            i.glyphicon.glyphicon-pencil  
          a(href="/admin/user/remove/"+user.id)
            i.glyphicon.glyphicon-remove